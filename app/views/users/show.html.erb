<p id="notice"><%= notice %></p>
<div class="container">
    <div class="row">
        <div class="col s12 m4">
          <p>
            <strong>Name:</strong>
            <%= @user.name %>
          </p>

          <p>
            <strong>Email:</strong>
            <%= @user.email %>
          </p>
          </div>

    <div class="col s12 m8">
    <%= image_tag(@user.image, height: '400', width: '400') %>
    <p>
      <strong>Admin:</strong>
      <%= @user.admin %>
    </p>

    <%= link_to 'Edit', edit_user_path(@user) %> |
    <%= link_to 'Back', users_path %>
    </div>
